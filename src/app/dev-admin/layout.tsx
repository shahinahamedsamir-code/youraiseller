export const metadata = {
  title: "Developer Control",
  robots: { index: false, follow: false },
};

export default function DevAdminRootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
